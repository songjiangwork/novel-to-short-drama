# v1.2-A5C — Fact Semantic Packing v1（冻结后置审计策略）

状态：**FROZEN POST-AUDIT POLICY**

适用 Issue：

```text
#52 — A5C fact semantic ambiguity resolution
```

权威来源：

```text
A5C-A Alice zero-provider request-shape audit
→ architecture review
→ fact-semantic-packing-v1 FROZEN at 12 / 24
```

本文件冻结 production fact semantic packing policy。A5C-B 必须消费本策略，
不得重新选择 P1 / P2 / P3。provider smoke 只**验证**本策略的实现，**不动态
选择** packing policy。

Alice 冻结输入（A5C-A audit）：

```text
fact candidates = 158
semantic fact pairs = 2113
auto_same = 0
```

---

# 1. Frozen policy

```text
policy:
  fact-semantic-packing-v1

max_pairs_per_block:
  12

max_candidates_per_block:
  24
```

This corresponds to audit candidate **P2**. P1 and P3 are NOT selected.

The canonical packing material is the frozen **behavioral limits**
(`12` / `24`), not the human policy label. See section 6.

---

# 2. Rationale (Alice zero-provider audit)

Observed zero-provider request shape:

```text
P1 — 6 pairs / 12 candidates
blocks = 353
rendered prompt:
  median = 12082 bytes
  p95    = 13220 bytes
  max    = 14676 bytes

P2 — 12 pairs / 24 candidates
blocks = 177
rendered prompt:
  median = 20917 bytes
  p95    = 22875 bytes
  max    = 25431 bytes

P3 — 24 pairs / 48 candidates
blocks = 89
rendered prompt:
  median = 38589 bytes
  p95    = 42250 bytes
  max    = 46192 bytes
```

Freeze P2 because:

```text
P1:
safe but requires 353 provider blocks.

P2:
cuts provider round trips approximately in half versus P1,
while requiring at most 12 structured decisions per response.

P3:
cuts calls further, but doubles the per-response decision count to 24
and materially increases structured-output completeness risk under the
tracked consolidation-llm-v1 max_output_tokens = 4096.
```

This is a precision / reliability-first v1 choice. P3 is NOT claimed to be
unable to work — it is simply not selected for v1 without evidence that the
larger response surface is worth the added structured-output risk.

---

# 3. Frozen packing algorithm

```text
1. consume A5B needs_semantic_decision fact pairs
   in canonical (left_ref, right_ref) order;

2. append greedily;

3. before appending the next pair, close the current block iff:
     pair_count + 1 > 12
   OR
     unique_candidate_count_after_append > 24;

4. never split a pair;

5. never reorder for packing efficiency;

6. no random packing;

7. no graph / community clustering.
```

Both limits are independent; a block closes when either would be exceeded.
No pair is split, lost, duplicated, or reordered. Block order is the
deterministic append order.

---

# 4. Candidate refs (frozen)

```text
candidate_refs = stable unique union of the block's pair endpoints

ordered by:
  source_order_key
  then candidate_ref
```

Set iteration order is forbidden. `candidate_refs` is used only for block
identity / audit / deterministic bookkeeping; the provider semantic context
stays pair-local endpoint packets (never a block-wide candidate or evidence
authority).

---

# 5. Block identity material (frozen)

```text
plan_hash
packing policy limits        (max_pairs_per_block, max_candidates_per_block)
block ordinal
ordered requested pairs
ordered candidate refs
```

Prefix:

```text
a5fblk_
```

`block_id = "a5fblk_" + first 20 hex of the shared content_hash` over the
block identity material above. The policy **name is not** part of the identity
material (only the two behavioral limits are), so an identical-behavior policy
with a different label produces identical block ids and request hashes.

---

# 6. Policy label vs semantic material

The human / versioned policy name is:

```text
fact-semantic-packing-v1
```

The canonical packing material is the frozen **behavioral limits**:

```text
max_pairs_per_block = 12
max_candidates_per_block = 24
```

A5C-B may construct:

```python
FactSemanticPackingPolicy(
    "fact-semantic-packing-v1",
    12,
    24,
)
```

and it MUST retain the same behavioral packing / canonical identity as the
audited P2 limits (identical block ids and request hashes). Do NOT change
block / request hashes merely to replace the audit label `P2` with the
production policy name.

---

# 7. Pair-local evidence semantics (frozen)

```text
no block-wide evidence pool

left indexed evidence:
L0, L1, ...

right indexed evidence:
R0, R1, ...
```

The exact A5B indexed evidence order is preserved. Left `L0` and right `R0` may
resolve to the same exact `EvidenceRef` while remaining two distinct selector
positions at preparation time.

---

# 8. Provider smoke does NOT choose the packing policy

```text
authority:
  Alice zero-provider audit
  → architecture review
  → fact-semantic-packing-v1 frozen as 12 / 24
```

A future provider smoke **validates the frozen policy implementation**; it does
NOT dynamically choose P1 / P2 / P3. If real provider execution later proves
`12 / 24` structurally unsuitable, that requires an explicit refinement /
policy version change — do not silently tune block size at runtime.

---

# 9. Frozen summary

1. `fact-semantic-packing-v1` = **12 pairs / 24 candidates** (audit candidate P2).
2. The production packing policy is FROZEN; it is no longer unresolved.
3. The frozen material is the two behavioral limits, not the policy label.
4. Block ids / request hashes are unchanged from the audited P2 preparation.
5. A5C-B consumes this policy; it does not re-select P1 / P2 / P3.
6. A provider smoke validates the implementation; it does not choose the policy.
