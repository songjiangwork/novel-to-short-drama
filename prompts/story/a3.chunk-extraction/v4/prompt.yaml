# v4 A3A Prompt Registry — a3.chunk-extraction v4 (TRACKED).
#
# v4 adds an explicit relationship distinct-entity rule (source_ref and
# target_ref MUST reference different candidate IDs; reflexive speech/thought
# is not a relationship) and changes the excerpt policy to null-preferred
# (uncertainty results in null, not an approximate excerpt). All v3 rules
# (anti-ellipsis, anti-duplication, exact-substring contract) are preserved.
#
# The pinned content_hash is computed over the exact semantic prompt material
# (system.txt + user.txt + sorted required_variables) via the A-I3 canonical
# prompt hash helper, NOT over this metadata file.
schema_version: 1
prompt_id: a3.chunk-extraction
version: 4
required_variables:
  - chunk_id
  - left_context_json
  - ownership_json
  - right_context_json
content_hash: e9c4578443040f061d2774d9cdbace6278d04ec0c69edf00652b490cb505a150
