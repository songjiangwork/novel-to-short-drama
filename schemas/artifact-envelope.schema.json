{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "artifact-envelope.schema.json",
  "title": "ImmutableArtifactEnvelope",
  "description": "content_hash is SHA-256 over RFC 8785 canonical bytes of an object containing exactly schema_version and payload. artifact_type, artifact_id, revision, and content_hash itself are excluded from the hash input.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "artifact_type",
    "artifact_id",
    "revision",
    "schema_version",
    "content_hash",
    "payload"
  ],
  "properties": {
    "artifact_type": {"type": "string", "minLength": 1, "pattern": "^[^\\u0000\\uD800-\\uDFFF]+$"},
    "artifact_id": {"type": "string", "minLength": 1, "pattern": "^[^\\u0000\\uD800-\\uDFFF]+$"},
    "revision": {"type": "integer", "minimum": 1},
    "schema_version": {"type": "integer", "minimum": 1},
    "content_hash": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "payload": {"description": "Must be representable by the RFC 8785 JSON Canonicalization Scheme."}
  }
}
