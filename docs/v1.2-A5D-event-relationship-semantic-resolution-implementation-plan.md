# v1.2-A5D — Event + Relationship Semantic Resolution Implementation Plan（冻结前置审计版）

状态：**FROZEN PRE-AUDIT CONTRACT**

适用 Issue：

```text
#53 — A5D event and relationship semantic ambiguity resolution
```

当前 merged baseline：

```text
main = 4e0ac6cf2016be37d1df3750c40fdfe4c5969028
A5A = COMPLETE
A5B = COMPLETE
A5C = COMPLETE
```

Alice production planning authority：

```text
EventCandidate count          = 167
event explicit pairs          = 2941
event auto_same               = 0
event semantic pairs          = 2941

RelationshipCandidate count   = 116
relationship explicit pairs   = 428
relationship auto_same        = 0
relationship semantic pairs   = 428
```

本计划细化 A5D，不重新设计 A5A/A5B/A5C，不提前实现 A5E/A5F。

---

# 1. Authoritative inputs

A5D 必须消费 A5B 的 deterministic planning result：

```text
ConsolidationPlanningResult
  ├── index
  ├── event_pair_plans
  ├── relationship_pair_plans
  ├── deterministic_decision_set
  └── plan_hash
```

A5D 只处理：

```text
EventPairPlan.state == needs_semantic_decision
RelationshipPairPlan.state == needs_semantic_decision
```

不得重新 blocking、重新枚举 pair、补比较未 blocked pair，或重新解释 A5B pair signals。

A5B 的 deterministic auto_same decisions 保持 authoritative，不再次发给 provider。

---

# 2. Existing A5A assets are authoritative

必须复用已有 contract，不得创建第二套 domain model。

Event：

```text
EventSemanticDecision
EventSelectorDecisionItem
EventSelectorDecisionPayload
```

Relationship：

```text
RelationshipSemanticDecision
RelationshipSelectorDecisionItem
RelationshipSelectorDecisionPayload
```

Shared：

```text
ConsolidationDecisionSet
ConsolidationProfile
ConsolidationSemanticPass
```

Tracked orchestration profile：

```text
profiles/consolidation_v1.yaml
```

Event pins：

```text
semantic_profile_id = consolidation-llm-v1
prompt_id = a5.event-consolidation
prompt_version = 1
output_schema_id = consolidation-event-selector-payload
output_schema_version = 1
```

Relationship pins：

```text
semantic_profile_id = consolidation-llm-v1
prompt_id = a5.relationship-consolidation
prompt_version = 1
output_schema_id = consolidation-relationship-selector-payload
output_schema_version = 1
```

Shared semantic generation contract：

```text
temperature = 0.0
max_output_tokens = 4096
structured_output_mode = json_schema
reasoning.enabled = false
max_generation_rounds = 2
```

---

# 3. Frozen semantic decision domains

Event EXACT：

```text
same_event
different_event
uncertain
```

Only `same_event` can later create an event identity merge edge.

`different_event` means only “not the same event”; it is **not** StoryConflict.

`uncertain` is a successful semantic result and must not be retried merely to force certainty.

Relationship EXACT：

```text
same_relationship
different_relationship
uncertain
```

Only `same_relationship` can later create a relationship identity merge edge.

Relationship identity and relationship state remain separate:

```text
state_zh may be semantic evidence
state_zh is not the sole identity authority
```

Direction semantics remain authoritative:

```text
directed
symmetric
unknown
```

A5E owns final graph construction / endpoint component semantics / state-history assembly.

---

# 4. A5D split

Issue #53 is implemented in two sequential slices.

## A5D-A — zero-provider preparation + dual Alice packing audit

Implement both domains through:

```text
A5B semantic pair stream
→ deterministic domain-specific packing
→ pair-local endpoint packets
→ PromptRegistry render
→ OutputSchema binding
→ StructuredGenerationRequest
→ stable request_hash
→ Alice request-shape audit
```

A5D-A:

```text
ZERO provider calls
ZERO EventSemanticDecision(method=llm)
ZERO RelationshipSemanticDecision(method=llm)
ZERO persistence
ZERO CURRENT writes
```

A5D-A ends with a report and STOP.

Then architecture review separately freezes:

```text
event-semantic-packing-v1
relationship-semantic-packing-v1
```

## A5D-B — provider execution + semantic validation

Starts only after both packing policies are frozen.

Provider path per domain:

```text
prepared requests
→ LLMClient.generate_structured(...)
→ provenance verification
→ typed selector payload
→ exact requested-pair coverage
→ selector validation
→ canonical selector ordering
→ exact EvidenceRef resolution
→ stable exact-EvidenceRef dedupe
→ domain SemanticDecision(method=llm)
→ exact whole-domain decision coverage
```

---

# 5. Module boundary

Extend the existing:

```text
src/short_drama/story/consolidation_semantic.py
```

A5D should reuse proven A5C generic patterns where appropriate, but must not alter A5C fact semantics or fact request identity.

A5D may introduce small shared internal helpers only when behavior is truly identical across fact/event/relationship; do not perform a broad refactor as a prerequisite.

No A4 reconciliation-specific domain types may become A5 authorities.

---

# 6. A5D-A preparation results

Use immutable in-memory domain-specific preparations, e.g.:

```text
EventSemanticPreparation
RelationshipSemanticPreparation
```

Each must pin at least:

```text
planning_result
blocks
structured_requests
semantic_request_hashes

prompt_id
prompt_version
prompt_content_hash

output_schema_id
output_schema_version
output_schema_hash

semantic_profile_id
semantic_profile_hash

plan_hash
packing_policy
semantic_pair_count
auto_same_pair_count
total_pair_count
```

No persistence.

Same exact plan + same packing behavior + same tracked semantic assets must produce byte-stable preparation identity and request hashes.

---

# 7. Domain-specific endpoint packets

Event endpoint packet MUST derive only from exact `IndexedEventCandidate`:

```text
candidate_ref
summary_zh
participants
locations
temporal_mode
evidence_strength
source_order_key
evidence
```

Pair context also carries exact A5B `EventPairPlan.signals`.

Relationship endpoint packet MUST derive only from exact `IndexedRelationshipCandidate`:

```text
candidate_ref
source_entity_ref
target_entity_ref
relationship_type_zh
state_zh
direction
evidence_strength
source_order_key
evidence
```

Pair context also carries exact A5B `RelationshipPairPlan.signals`.

Do not add whole-novel context, third candidates, fuzzy name lookup, inferred chronology, or block-wide evidence pools.

---

# 8. Pair-local evidence selector authority

Both domains use the existing A5 selector authority:

```text
L0, L1, ...
R0, R1, ...
```

Left endpoint:

```text
evidence_refs[0] -> L0
evidence_refs[1] -> L1
...
```

Right endpoint:

```text
evidence_refs[0] -> R0
evidence_refs[1] -> R1
...
```

A5D-A establishes deterministic selector universes but performs no provider-result selector validation.

If L0 and R0 resolve to the same exact EvidenceRef, they remain distinct legal request-time selector positions.

No selector alias dedupe during preparation.

---

# 9. Canonical semantic stream

For each domain, consume only A5B `needs_semantic_decision` plans in canonical pair order:

```text
(left_ref, right_ref)
left_ref < right_ref
```

Validate before packing:

- correct candidate namespace;
- no duplicate pair;
- pair order canonical;
- only known pair-plan states;
- semantic stream equals exactly the A5B `needs_semantic_decision` subset;
- auto_same excluded;
- all endpoints exist in that exact domain index.

No heuristic reorder for packing efficiency.

---

# 10. Deterministic packing model

Event and relationship packing are independent policies.

Suggested immutable value:

```text
SemanticPackingPolicy
  name
  max_pairs_per_block
  max_candidates_per_block
```

A5D-A must NOT expose a production default before post-audit review.

For each domain, audit at least:

```text
P1:
  max_pairs_per_block = 6
  max_candidates_per_block = 12

P2:
  max_pairs_per_block = 12
  max_candidates_per_block = 24

P3:
  max_pairs_per_block = 24
  max_candidates_per_block = 48
```

These are audit candidates only.

Event and relationship are allowed to freeze different production policies after review.

---

# 11. Frozen packing algorithm

For every audit candidate and each domain:

```text
1. consume semantic pairs in canonical pair order;
2. greedily append;
3. before appending a pair, close the current block iff:
     pair_count + 1 > max_pairs_per_block
   OR
     unique_candidate_count_after_append > max_candidates_per_block;
4. never split a pair;
5. never reorder pairs;
6. no empty block;
7. deterministic block order;
8. no random / graph / community packing.
```

Candidate refs inside a block:

```text
stable unique union of pair endpoints
ordered by:
  source_order_key
  then candidate_ref
```

Candidate refs are identity/audit/bookkeeping only; semantic context remains pair-local.

---

# 12. Domain-specific block identity

Use distinct prefixes:

```text
event:
  a5eblk_

relationship:
  a5rblk_
```

Hash material must include:

```text
plan_hash
domain
packing policy behavioral limits
block_ordinal
ordered_requested_pairs
ordered_candidate_refs
```

Use shared canonical `content_hash`; first 20 hex chars.

Human audit candidate labels such as P1/P2/P3 are not semantic identity.

Do not include:

- provider / model / endpoint;
- timeout / credentials;
- llama.cpp slot;
- wall clock / PID;
- filesystem path.

---

# 13. Prompt / schema / profile verification

Before any request construction, fail closed unless:

Event:

```text
ConsolidationProfile.event semantic profile id
== loaded SemanticLLMProfile.profile_id

event prompt id/version
== loaded PromptRegistry asset

event output schema id/version
== loaded OutputSchema
```

Relationship: same checks against relationship pins.

Also require:

```text
max_generation_rounds == 2
```

Any mismatch:

```text
FAIL CLOSED
ZERO provider calls
```

---

# 14. Structured request identity

Every block must become a real shared A-I3:

```text
StructuredGenerationRequest
```

Request identity is backend-neutral and determined only by semantic/result-affecting material:

- tracked semantic profile;
- tracked prompt;
- deterministic pair context;
- tracked output schema.

Runtime routing identity must not enter request/reuse identity.

---

# 15. Alice A5D-A dual audit

Add a read-only zero-provider audit script, suggested:

```text
scripts/a5d_event_relationship_request_shape_audit.py
```

Input:

```text
runs/a4e_real_novel
project_id = a3e-real-novel
document_id = src_001
reconciliation_profile_id = entity-reconciliation-v2
consolidation profile = profiles/consolidation_v1.yaml
```

Exact expected production plan:

```text
events:
  candidates = 167
  explicit pairs = 2941
  auto_same = 0
  semantic pairs = 2941

relationships:
  candidates = 116
  explicit pairs = 428
  auto_same = 0
  semantic pairs = 428
```

Flow:

```text
validated A4 CURRENT
→ A5B planning
→ exact event semantic stream
→ exact relationship semantic stream
→ P1/P2/P3 preparation for each domain
→ render real StructuredGenerationRequests
→ report
→ STOP
```

Must prove:

```text
provider calls = 0
A5 writes = 0
CURRENT writes = 0
run tree unchanged
```

---

# 16. Required audit metrics

For Event and Relationship separately, for each P1/P2/P3:

```text
packing name
max_pairs_per_block
max_candidates_per_block
block_count
total_pairs_in_blocks

pairs_per_block:
  min median p95 max

unique_candidates_per_block:
  min median p95 max

evidence_items_per_block:
  min median p95 max

pair_contexts_json_bytes:
  min median p95 max

rendered_prompt_bytes:
  min median p95 max

request_hash_count
unique_request_hash_count

largest_block:
  block_id
  block_ordinal
  pair_count
  unique_candidate_count
  evidence_item_count
  pair_contexts_json_bytes
  rendered_prompt_bytes
  first_pair
  last_pair
```

Also print domain semantic asset identity and plan_hash.

Use deterministic nearest-rank p95 consistent with A5C-A.

No provider tokenizer required.

---

# 17. Determinism gates

Build each preparation twice over the same exact run tree.

Require identical:

```text
block ids
block pair membership
candidate_refs
pair_contexts_json
structured request hashes
audit metrics
```

No random sampling.

---

# 18. A5D-A tests

At minimum cover both domains:

## semantic stream
- only needs_semantic_decision;
- auto_same excluded;
- exact pair coverage;
- canonical pair order;
- duplicate / wrong namespace fail closed.

## endpoint packets
- exact indexed fields;
- exact evidence order;
- exact signals;
- source_order_key preserved;
- no third candidate;
- no block-wide evidence authority.

## selectors in request
- L#/R# map to exact endpoint evidence positions;
- same exact EvidenceRef on both sides remains two legal request-time positions;
- no selector alias dedupe during preparation.

## packing
- pair limit;
- independent unique-candidate limit;
- deterministic greedy boundary;
- no pair split/loss/duplication;
- stable candidate_refs;
- stable domain-specific block ids.

## assets
- exact prompt/schema/profile pins;
- max_generation_rounds == 2;
- mismatch fail closed;
- zero provider calls.

## hashes
- same input → same request hashes;
- plan_hash change → identity changes;
- endpoint semantic/evidence change → request hash changes;
- runtime/provider/model/timeout absent.

---

# 19. A5D-A explicit out of scope

Do NOT implement:

- provider invocation;
- semantic retry loop;
- EventSelectorDecisionPayload result parsing;
- RelationshipSelectorDecisionPayload result parsing;
- selector result validation/resolution;
- LLM EventSemanticDecision / RelationshipSemanticDecision;
- decision coverage merge;
- event same graph;
- relationship identity graph;
- canonical IDs;
- relationship state histories;
- StateTransition;
- StoryConflict;
- persistence;
- ValidationReport;
- CURRENT;
- reuse;
- CLI;
- A6.

---

# 20. A5D-B reserved contract

After both packing policies are post-audit frozen, A5D-B must reuse the exact A5D-A preparation path.

For each domain:

```text
prepared requests
→ generate_structured
→ provenance verification
→ typed selector payload
→ exact pair/order coverage
→ pair-local selector validation
→ canonical selector order
→ exact endpoint EvidenceRef resolution
→ stable exact-EvidenceRef dedupe
→ domain SemanticDecision(method=llm)
→ exact whole-domain coverage
```

Semantic rounds:

```text
max = 2
```

Semantic-invalid results may consume a round.

```text
decision == uncertain
```

is successful and must not be retried.

Provenance mismatch:

```text
FAIL CLOSED
NO semantic retry
```

Technical A-I3 `LLMError` remains an A-I3 boundary and is not converted into a semantic retry.

---

# 21. Post-audit freeze gate

A5D-A must STOP after the dual audit.

Architecture review then freezes separately:

```text
event-semantic-packing-v1:
  max_pairs_per_block
  max_candidates_per_block

relationship-semantic-packing-v1:
  max_pairs_per_block
  max_candidates_per_block
```

Both also inherit the frozen algorithm, candidate ordering, block identity material and endpoint packet schema from this contract.

A future provider smoke validates a frozen policy; it does not dynamically select P1/P2/P3.

If audit data says none of the candidates are reasonable for a domain, perform an explicit refinement instead of provider-driven trial-and-error.

---

# 22. Validation for A5D-A

Run focused A5A/A5B/A5C/A5D-A tests, then:

```bash
python -m compileall -q src scripts tests
git diff --check
pytest tests/
```

Then:

```bash
python scripts/a5d_event_relationship_request_shape_audit.py
```

Expected:

```text
provider calls = 0
run tree unchanged
```

---

# 23. Frozen PRE-AUDIT summary

1. #53 remains one A5D issue but is implemented as A5D-A then A5D-B.
2. A5D-A is zero-provider preparation + separate Event/Relationship request-shape audits.
3. Event and Relationship retain separate semantic domains and may freeze different packing policies.
4. A5D reuses A5A models, A5B pair plans and shared A-I3 infrastructure.
5. Pair context is endpoint-only and pair-local; no block-wide evidence pool.
6. Exact A5B source order and evidence order remain authoritative.
7. Event decisions are exactly same_event/different_event/uncertain.
8. Relationship decisions are exactly same_relationship/different_relationship/uncertain.
9. different_event is not StoryConflict.
10. relationship identity and state remain separate.
11. semantic uncertainty is successful output.
12. A5D-A performs zero provider calls and zero persistence.
13. Both production packing policies remain unresolved until the Alice dual audit is reviewed.
14. A5D-B begins only after both policies are explicitly frozen.
15. A5D does not perform final graph construction, canonical IDs, state histories, persistence or CURRENT.
