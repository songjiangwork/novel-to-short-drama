# v1.2-A Implementation Task Map（冻结设计对应）

## A1 — Source Ingestion

实现：

```text
TXT + text-based PDF
→ immutable SourceDocument
```

核心：

- SHA256 revision
- source language detection
- deterministic chapter parsing
- paragraph IDs
- JSON schema
- fail-closed validation

---

## A2 — Chunk Planning

实现：

```text
SourceDocument
→ context/ownership chunks
→ ChunkManifest
```

核心：

- token/paragraph-aware chunking
- exact-one ownership invariant
- coverage audit
- deterministic chunk IDs

---

## A3 — Chunk Extraction

实现：

```text
Chunk
→ Python orchestration
→ local Qwen
→ validated candidate extraction
```

核心：

- versioned prompt
- OpenAI-compatible LLM adapter
- JSON structured output
- source-ref validation
- semantic validation
- retry / resume / cache

---

## A4 — Entity Reconciliation

实现：

```text
Candidate characters/locations
→ deterministic blocking
→ Qwen ambiguity resolution
→ graph canonicalization
```

核心：

- precision-first merge
- must-not-merge
- same/different/uncertain
- entity_map
- stable canonical IDs
- explicit unresolved

---

## A5 — Fact / Event Consolidation

实现：

```text
candidate facts/events
+
entity_map
→ canonical facts/events/relationships/conflicts
```

核心：

- deterministic dedupe/blocking
- semantic classifications
- graph event consolidation
- relationship state history
- state change vs conflict
- narrative order

---

## A6 — Global Story Bible

实现：

```text
canonical evidence
→ hierarchical global analysis
→ story_bible.yaml
```

核心：

- character dossiers
- plot windows
- global skeleton
- final synthesis
- full-story coverage after compression
- global arcs / turning points / reveals
- no adaptation decisions
- cross-reference validation

---

# Top-level future CLI

开发阶段保留分阶段 CLI。

最终目标：

```bash
short-drama analyze-story project.yaml
```

自动执行：

```text
A1 → A2 → A3 → A4 → A5 → A6
```

支持：

- resume
- validated artifact reuse
- provider override by semantic capability
- future ComfyUI frontend
