# v1.2 A3A Prompt Registry — a3.chunk-extraction v2 (TRACKED).
#
# v2 strengthens the evidence excerpt contract: exact contiguous substring
# requirement, explicit null fallback, short-verbatim preference, and
# self-check instruction. All v1 rules are preserved.
#
# The pinned content_hash is computed over the exact semantic prompt material
# (system.txt + user.txt + sorted required_variables) via the A-I3 canonical
# prompt hash helper, NOT over this metadata file.
schema_version: 1
prompt_id: a3.chunk-extraction
version: 2
required_variables:
  - chunk_id
  - left_context_json
  - ownership_json
  - right_context_json
content_hash: 614e6bbb866b497b550cee1feb0dd9d955ec37ad97b209a2b7bd50a19ee0df06
